# yamllint configuration extending default
extends: default

rules:
  # 160 chars should be enough, but don't fail if a line is longer
  line-length:
    max: 120
    level: warning
  document-start:
    present: false
